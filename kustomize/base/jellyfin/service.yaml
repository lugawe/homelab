apiVersion: v1

kind: Service

metadata:
  namespace: jellyfin
  name: jellyfin-app-service
  labels:
    app: jellyfin

spec:
  selector:
    app: jellyfin
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8096
