apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: nextcloud
  name: app-pvc
  labels:
    app: app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
