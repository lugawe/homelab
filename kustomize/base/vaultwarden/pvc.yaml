apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  namespace: vaultwarden
  name: vaultwarden-app-pvc
  labels:
    app: vaultwarden-app

spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path
