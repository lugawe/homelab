apiVersion: v1

kind: Service

metadata:
  namespace: vaultwarden
  name: vaultwarden-app-service
  labels:
    app: vaultwarden

spec:
  selector:
    app: vaultwarden
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
